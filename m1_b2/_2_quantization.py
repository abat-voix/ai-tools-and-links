"""
# Установка Ollama (если ещё не установлен)
curl -fsSL https://ollama.com/install.sh | sh

# Скачивание и запуск Qwen 3 8B в квантизации Q4_K_M
# Размер: ~5 ГБ, работает на любом современном ноутбуке
ollama run qwen3:8b

# Проверка: какие модели скачаны и сколько занимают
ollama list
# NAME              SIZE      MODIFIED
# qwen3:8b          4.9 GB    2 minutes ago
"""

import ollama
import time

prompt = "Объясни разницу между REST API и GraphQL в 3 предложениях."

# Тестируем модель
start = time.time()
response = ollama.chat(
    # model="qwen3:8b",
    model="gemma3:1b",
    messages=[{"role": "user", "content": prompt}]
)
elapsed = time.time() - start

print(f"Время ответа: {elapsed:.1f} сек")
print(f"Токенов в ответе: {response['eval_count']}")
print(f"Скорость: {response['eval_count'] / elapsed:.1f} tok/s")
print(f"\nОтвет:\n{response['message']['content']}")

"""
Время ответа: 11.1 сек
Токенов в ответе: 341
Скорость: 30.7 tok/s

Ответ:
REST API использует стандартные HTTP-методы и возвращает данные по отдельным ресурсам, в то время как GraphQL позволяет клиенту точно указывать, какие данные ему нужны и как их структурировать. 
REST требует нескольких запросов для сбора информации, а GraphQL объединяет данные в один запрос, снижая нагрузку на сеть. 
GraphQL предоставляет больше гибкости в запросах, но может быть сложнее в реализации и оптимизации по сравнению с REST.
"""


"""
Время ответа: 2.2 сек
Токенов в ответе: 140
Скорость: 63.2 tok/s

Ответ:
Вот 3 предложения, объясняющие разницу между REST API и GraphQL:

1.  **REST API** - это более традиционный подход, который предоставляет набор конечных точек, которые возвращают данные в виде HTML-файлов.
2.  **GraphQL** -  это более современный подход, который позволяет клиенту (например, веб-приложению) конкретно запрашивать только те данные, которые ему нужны, избегая ненужных запросов.
3.  **В итоге, REST API  более гибкий для множества сценариев, в то время как GraphQL  предоставляет более точный и эффективный запрос на данные.**

"""

